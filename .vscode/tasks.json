{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-prompt", // Matches the "preLaunchTask" in launch.json
            "type": "shell",
            "command": "g++",
            "args": [
                "-std=c++20",
                // "-pthread",
                "${workspaceFolder}/src/prompt.cpp",
                "-o", "${workspaceFolder}/builds/prompt.gdb",
                "-g", "-O0", "-fsanitize=address", "-DTEST", //"-DTEST_FAILURE_DIES",
                // "-Ofast",

                "-pedantic-errors",
                "-Werror", // Treats all warnings as errors, forcing the compiler to flag suspicious code.
                "-Wall", "-Wextra", // Enables extra warnings, including unused or suspicious constructs.
                "-Wunused", // Warns if a function or variable is unused, which might hint at optimization skipping drop.
                "-fno-elide-constructors", // Disables copy elision, which could hide template-related bugs.
                // "-fsyntax-only", // Checks syntax without generating code, forcing full parsing.
                // "-Wundefined-func-template", // (Clang): Warns about undefined template functions.

                "-I${workspaceFolder}/libs/ggerganov/whisper.cpp/include",
                "-I${workspaceFolder}/libs/ggerganov/whisper.cpp/ggml/include",
                // "${workspaceFolder}/libs/ggerganov/whisper.cpp/build/src/libwhisper.so.1.7.4", 
                "-Wl,-rpath,${workspaceFolder}/libs/ggerganov/whisper.cpp/build/src",  // <-- Add this
                "-lrt", "-lm", "-lasound", //"-ljack", 
                "-lportaudio", //"-lwhisper",
                "-lcurl",
                "-pthread"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Generated by Debug Configuration"
        }
    ]
}