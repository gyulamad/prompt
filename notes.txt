# build && run
g++ -std=c++23 prompt.cpp -o prompt && ./prompt

# deploy
cp ./prompt ~/bin/prompt

# build && deploy
g++ -std=c++23 prompt.cpp -o prompt && cp ./prompt ~/bin/prompt

# build && deploy && run
g++ -std=c++23 prompt.cpp -o prompt && cp ./prompt ~/bin/prompt && ./prompt

# test
g++ -std=c++23 tests/tests.cpp -o tests/tests && tests/tests


# test debug
g++ -std=c++23 tests/tests.cpp -o tests/tests.gdb -g && tests/tests.gdb
