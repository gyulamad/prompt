#pragma once

#include <string>

using namespace std;

namespace tools::str {

    // Check if a string contains a substring
    bool str_contains(const string& str, const string& substring);

} // namespace tools::str